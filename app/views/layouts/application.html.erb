<!DOCTYPE html>
<html>
<head>
  <title>JobsRubyTw</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
<h1><a href="/">Job board for Ruby and Rails developers in Taiwan</a></h1>

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<p>
<% if user_signed_in? %>
	Hi, <%= current_user.email %> 
	<%= link_to "List my posting", user_jobs_path( current_user ) %>
	<%= link_to "Logout", destroy_user_session_path %>
<% else %>
	<%= link_to "Sign up", new_registration_path(:user) %> | 
	<%= link_to "Login", new_session_path(:user) %>
<% end %>
</p>

<%= yield %>

<div id="footer">
	
<p>	The job board is a free service by <a href="http://ruby.tw">Ruby Taiwan</a>.</p>

<p>
	<%= link_to "FAQ", about_path %> | <a href="#">Subscribe to the RSS feed</a> | <a href="http://twitter.com/rubytaiwan">follow @RubyTaiwan on Twitter</a>
</p>
	
</div>

</body>
</html>
